{
  "name": "guia-cultivo-push-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "vapid": "node -e \"import('@block65/webcrypto-web-push').then(m => m.generateVAPIDKeys().then(k => console.log('PUBLIC:', k.publicKey, '\\nPRIVATE:', k.privateKey)))\""
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.0",
    "wrangler": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
